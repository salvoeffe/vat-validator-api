# Server (Vercel sets PORT automatically)
# PORT=3000

# Rate limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=100

# Optional: require API key for validation endpoints (e.g. for RapidAPI)
# When set, clients must send X-API-Key header or Authorization: Bearer <key>
# API_KEY=your-secret-key

# Node environment (development | production)
# NODE_ENV=development
